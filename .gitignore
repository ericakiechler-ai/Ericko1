# The signing key. Losing it ends your ability to issue licences; leaking it
# lets anyone forge one. It must never enter version control.
keys/signing-key.pem

# Per-customer builds and the register of who holds what.
dist/845-VEC-*.html
!dist/845-VEC-unlicensed.html
licences/

node_modules/

# Generated at build time from the licensed dist file — never a tracked artifact,
# because it carries a specific customer's licence.
desktop/frontend/index.html
